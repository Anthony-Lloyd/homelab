version: "3.8"

services:
  sonarr:
    image: linuxserver/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
    volumes:
      - /mnt/sas_dataset/torrents:/downloads
      - /mnt/sas_dataset/torrents/television:/tv
    ports:
      - 8989:8989
    restart: unless-stopped
