version: "3.8"

services:
  radarr:
    image: linuxserver/radarr:latest
    container_name: radarr
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/London
    volumes:
      - /mnt/sas_dataset/torrents:/downloads
      - /mnt/sas_dataset/torrents/film:/movies
    ports:
      - 7878:7878
    restart: unless-stopped
